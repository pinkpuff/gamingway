def set_constants(main):
 if len(main.spells) > 0:
  main.NO_SPELL = main.spells[0]
 if len(main.spells) > 1:
  main.HOLD_SPELL = main.spells[1]
 if len(main.spells) > 2:
  main.MUTE_SPELL = main.spells[2]
 if len(main.spells) > 3:
  main.CHARM_SPELL = main.spells[3]
 if len(main.spells) > 4:
  main.BLINK_SPELL = main.spells[4]
 if len(main.spells) > 5:
  main.ARMOR_SPELL = main.spells[5]
 if len(main.spells) > 6:
  main.SHELL_SPELL = main.spells[6]
 if len(main.spells) > 7:
  main.SLOW_SPELL = main.spells[7]
 if len(main.spells) > 8:
  main.FAST_SPELL = main.spells[8]
 if len(main.spells) > 9:
  main.BERSK_SPELL = main.spells[9]
 if len(main.spells) > 10:
  main.WALL_SPELL = main.spells[10]
 if len(main.spells) > 11:
  main.WHITE_SPELL = main.spells[11]
 if len(main.spells) > 12:
  main.DISPL_SPELL = main.spells[12]
 if len(main.spells) > 13:
  main.PEEP_SPELL = main.spells[13]
 if len(main.spells) > 14:
  main.CURE1_SPELL = main.spells[14]
 if len(main.spells) > 15:
  main.CURE2_SPELL = main.spells[15]
 if len(main.spells) > 16:
  main.CURE3_SPELL = main.spells[16]
 if len(main.spells) > 17:
  main.CURE4_SPELL = main.spells[17]
 if len(main.spells) > 18:
  main.HEAL_SPELL = main.spells[18]
 if len(main.spells) > 19:
  main.LIFE1_SPELL = main.spells[19]
 if len(main.spells) > 20:
  main.LIFE2_SPELL = main.spells[20]
 if len(main.spells) > 21:
  main.SIZE_SPELL = main.spells[21]
 if len(main.spells) > 22:
  main.EXIT_SPELL = main.spells[22]
 if len(main.spells) > 23:
  main.SIGHT_SPELL = main.spells[23]
 if len(main.spells) > 24:
  main.FLOAT_SPELL = main.spells[24]
 if len(main.spells) > 25:
  main.TOAD_SPELL = main.spells[25]
 if len(main.spells) > 26:
  main.PIGGY_SPELL = main.spells[26]
 if len(main.spells) > 27:
  main.WARP_SPELL = main.spells[27]
 if len(main.spells) > 28:
  main.VENOM_SPELL = main.spells[28]
 if len(main.spells) > 29:
  main.FIRE1_SPELL = main.spells[29]
 if len(main.spells) > 30:
  main.FIRE2_SPELL = main.spells[30]
 if len(main.spells) > 31:
  main.FIRE3_SPELL = main.spells[31]
 if len(main.spells) > 32:
  main.ICE1_SPELL = main.spells[32]
 if len(main.spells) > 33:
  main.ICE2_SPELL = main.spells[33]
 if len(main.spells) > 34:
  main.ICE3_SPELL = main.spells[34]
 if len(main.spells) > 35:
  main.LIT1_SPELL = main.spells[35]
 if len(main.spells) > 36:
  main.LIT2_SPELL = main.spells[36]
 if len(main.spells) > 37:
  main.LIT3_SPELL = main.spells[37]
 if len(main.spells) > 38:
  main.VIRUS_SPELL = main.spells[38]
 if len(main.spells) > 39:
  main.WEAK_SPELL = main.spells[39]
 if len(main.spells) > 40:
  main.QUAKE_SPELL = main.spells[40]
 if len(main.spells) > 41:
  main.SLEEP_SPELL = main.spells[41]
 if len(main.spells) > 42:
  main.STONE_SPELL = main.spells[42]
 if len(main.spells) > 43:
  main.FATAL_SPELL = main.spells[43]
 if len(main.spells) > 44:
  main.STOP_SPELL = main.spells[44]
 if len(main.spells) > 45:
  main.DRAIN_SPELL = main.spells[45]
 if len(main.spells) > 46:
  main.PSYCH_SPELL = main.spells[46]
 if len(main.spells) > 47:
  main.METEO_SPELL = main.spells[47]
 if len(main.spells) > 48:
  main.NUKE_SPELL = main.spells[48]
 if len(main.spells) > 49:
  main.IMP_SPELL = main.spells[49]
 if len(main.spells) > 50:
  main.BOMB_SPELL = main.spells[50]
 if len(main.spells) > 51:
  main.COCKA_SPELL = main.spells[51]
 if len(main.spells) > 52:
  main.MAGE_SPELL = main.spells[52]
 if len(main.spells) > 53:
  main.CHOCB_SPELL = main.spells[53]
 if len(main.spells) > 54:
  main.SHIVA_SPELL = main.spells[54]
 if len(main.spells) > 55:
  main.INDRA_SPELL = main.spells[55]
 if len(main.spells) > 56:
  main.JINN_SPELL = main.spells[56]
 if len(main.spells) > 57:
  main.TITAN_SPELL = main.spells[57]
 if len(main.spells) > 58:
  main.MIST_SPELL = main.spells[58]
 if len(main.spells) > 59:
  main.SYLPH_SPELL = main.spells[59]
 if len(main.spells) > 60:
  main.ODIN_SPELL = main.spells[60]
 if len(main.spells) > 61:
  main.LEVIA_SPELL = main.spells[61]
 if len(main.spells) > 62:
  main.ASURA_SPELL = main.spells[62]
 if len(main.spells) > 63:
  main.BAHAM_SPELL = main.spells[63]
 if len(main.spells) > 64:
  main.COMET_SPELL = main.spells[64]
 if len(main.spells) > 65:
  main.FLARE_SPELL = main.spells[65]
 if len(main.spells) > 66:
  main.FLAME_SPELL = main.spells[66]
 if len(main.spells) > 67:
  main.FLOOD_SPELL = main.spells[67]
 if len(main.spells) > 68:
  main.BLITZ_SPELL = main.spells[68]
 if len(main.spells) > 69:
  main.SMOKE_SPELL = main.spells[69]
 if len(main.spells) > 70:
  main.PIN_SPELL = main.spells[70]
 if len(main.spells) > 71:
  main.IMAGE_SPELL = main.spells[71]

